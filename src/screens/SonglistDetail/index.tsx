/**
 * SonglistDetail screen - kept for navigation registration compatibility.
 * TV entry point now goes through TVMusicDetail instead.
 */
import { View } from 'react-native'

export default () => {
  return <View />
}
